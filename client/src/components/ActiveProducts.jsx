import React from 'react';
import styled from 'styled-components';

let StyledActive = div`
  padding-left: 100px;
  padding-right: 100px;
`

const ActiveProducts = (props) => {
  <StyledActive>
    
  </StyledActive>
}

export default ActiveProducts